name: pathcache
category: pwn
author: VKACTF
description: |
  A low-privileged Windows service can ask a kernel driver to read files from
  its configured source directory. The flag is readable only by SYSTEM.
value: 500
type: dynamic
extra:
  initial: 500
  decay: 50
  minimum: 100
connection_info: nc {{host}} {{port}}
flags:
  - VKACTF{test_flag_replace_me}
topics:
  - windows
  - kernel
  - integer overflow
files:
  - handout/pathcache.zip
state: hidden
